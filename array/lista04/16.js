//Crie um array de números e use findIndex() para retornar o índice do primeiro número menor que 10.
    let numeros = [ 22,10,9,8];
    let indice = numeros.findIndex(numeros=>numeros < 10);

    console.log(indice)