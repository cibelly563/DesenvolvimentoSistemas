/**
 * condicionais
 * operador ternário
 */

let idade = 18
let isAdulto = (idade>= 18) ? 'sim' : 'não';

// a crase (aspas de lado) é quando ultiliza-se interpolação

console.log(`voce tem ${idade}.`);

// aspas é concatenação
console.log('é adulto?' +  isAdulto);

